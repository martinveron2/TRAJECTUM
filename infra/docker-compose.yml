services:
  api:
    build:
      context: ../backend/api
    command: uvicorn trajectum_api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      TRAJECTUM_ENV: local
